/**
 * Master are.na channels (are.na/tom). Each block in a master channel is a
 * child channel: one post or work. Connection position is display order, so
 * dragging a block in are.na reorders the site.
 */
export const ARENA_POSTS_CHANNEL_SLUG = "posts-fwj0hyy6cee";

/** Channel title is "Work" on are.na; the slug is the source of truth. */
export const ARENA_WORK_CHANNEL_SLUG = "work-usxogi1u-ta";
