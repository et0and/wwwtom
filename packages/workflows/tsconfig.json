{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // The shared config still sets `baseUrl`, which TypeScript 6 deprecates.
    "ignoreDeprecations": "6.0",
    "outDir": "./dist",
    "composite": false
  },
  "include": ["src/**/*.ts"]
}
